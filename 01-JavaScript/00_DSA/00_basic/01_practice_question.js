// Q1: Calculate area and parameter of rectangle
// let length = 0;
// let width = 0;

function result(length, width) {
    const area = length * width;
    const parameter = 2 * (length + width);

    return `Area is: ${area} and parameter is: ${parameter}`

}

const answer = result(12, 7);
console.log(answer);

// Q2 Generate OTP. 

// function GenerateOTP() {
//     console.log(Math.floot(Math.random() * 100))
// }
